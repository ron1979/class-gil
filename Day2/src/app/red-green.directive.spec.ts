import { RedGreenDirective } from './red-green.directive';

describe('RedGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new RedGreenDirective();
    expect(directive).toBeTruthy();
  });
});
